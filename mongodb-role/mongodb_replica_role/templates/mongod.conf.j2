rs.initiate({
  _id: "{{ mongodb_replset }}",
  members: [
    { _id: 0, host: "{{ mongodb_master_ip }}:{{ mongodb_port }}" },
    { _id: 1, host: "{{ mongodb_replica_ip }}:{{ mongodb_port }}" }
  ]
});
